# Decisions

| ID | Decision | Status | Date | Source/Rationale |
|----|----------|--------|------|-----------------|
| D1 | Kickoff brief approved: reproduce deceptive grounding (arXiv 2607.09349) judge-free on a fabricated corpus, reduced 2×2 factorial, M0–M3 phasing | Approved | 2026-07-15 | `docs/KICKOFF.md` — RANGE pick per the lineage selection bar; paper ships no code, detector made mechanical via token ownership |
| D2 | M0 design D1–D9 pre-committed (manipulation direction confirmed, live-pinned roster with slug deviations, seeded corpus, controlled retrieval, generator+verifier contracts, four detectors + outcome taxonomy, kill/swap triggers, $0.60 budget cap, flat-script conventions) | Approved | 2026-07-15 | `docs/M0-BRIEF.md` D1–D9 — gates written and committed before any paid call |
| D3 | M0 verdict: FIT — all three subjects survive; grounding at ceiling; DG 0/36 at the adversarial cell with all-K4 (robust-low-DG, right reason); M1 sized at 20 pairs | Approved | 2026-07-15 | `data/m0_verdict.json` + `docs/M0-BRIEF.md` addendum — rendered mechanically by `m0.py verdict` |
| D4 | Park the project post-M0; re-queue as default repro #6 (displaced at seed-hunt by the workspace/J-lens pick, dim-stage) | Approved | 2026-07-15 | Seed-hunt decision; repo + approved KICKOFF + M0 results intact for resume |
| D5 | M1 design: run as pre-committed (likely well-powered NULL headline) vs. documented camouflage-level variant (e.g. multi-doc retrieval with filler docs) | Resolved (by D6) | 2026-08-03 | Kyle's call at the M1 brief (`docs/M1-BRIEF.md`, written 2026-08-03 — argues A / B / C-both-sequenced, recommends C); flagged in `docs/M0-BRIEF.md` addendum — a design change must be argued, not slipped in |
| D6 | M1 runs as **Option C — both surfaces, sequenced**: M1a exactly as pre-committed, then M1b camouflage arm (JSON tool-result rendering, constant titles, k=4 off-theme fillers, misattributed/confab detector split) as a labeled extension; 20 pairs via seed-preserving corpus extension guarded by committed M0 fixtures (corpus/docs/gen_log); no trims | Approved | 2026-08-03 | Kyle's pick, in-session at `docs/M1-BRIEF.md` ("The pick" addendum); resolves D5; PR #8 |
| D7 | Price re-pin at `m1.py ping` before any M1 spend: `qwen-2.5-7b-instruct` (0.04,0.10)→(0.10,0.20) and `qwen3-14b` (0.10,0.24)→(0.2275,0.91). Roster unchanged — meter accuracy, not a design change; M0's recorded per-call costs stand as measured | Approved | 2026-08-03 | `m1.py ping` drift report, resolved consciously before spending (M0-BRIEF D2 live-pin rule) |
| D8 | `$0.45 M1 total` enforced as a hard ceiling on **measured** spend (`CAP_M1_TOTAL`, ledger `data/m1_spend.json`), each wave running under min(own cap, remainder) | Approved | 2026-08-03 | M1-BRIEF D4's table only sums to its stated $0.45 if the smoke row is counted once, while the row itself reads "per arm" ($0.50); the stated ceiling is what was pre-committed, so it is enforced directly rather than left to the sum of per-wave caps |
| D9 | Corpus extension built **before** M1a rather than inside the M1b build, as the brief's "Next, in order" list implied | Approved | 2026-08-03 | D4's table sizes M1a at 2 cells × 20 × 3 = 120 trials and opens "every option needs build work here"; M1a at 12 pairs would auto-report UNDERPOWERED against the N≥20 gate. Shared infrastructure, no other sequence change — recorded in the M1-BRIEF outcome addendum |
| D10 | **M1 verdict: NULL at both surfaces, well-powered.** 20/20 clean per gated cell per model on both arms; DG-Y 0/20 everywhere except qwen-2.5-7b at the camouflaged adversarial cell (2/20; Newcombe +0.100 [−0.077, +0.301], straddles 0). Flagship blindness contrast **rendered** on those 2 answers (faithfulness PASS 2/2, citation PASS 2/2). DG-any 0/120 under fillers. Total M1 spend $0.0177 / $0.45 | Approved | 2026-08-03 | Rendered mechanically by `m1.py verdict --arm a\|b`; `data/m1a_verdict.json`, `data/m1b_verdict.json`, `data/m1_surface_contrast.json`; outcome addendum in `docs/M1-BRIEF.md` |
| D11 | v1 closes at M1 — M2 (suppress a rate already ~0) and M3 (ablate a phenomenon that occurred twice) are degenerate on the measured data | Superseded (by D15) | 2026-08-03 | `docs/M1-BRIEF.md` outcome addendum "What this means for M2/M3"; D1 of that brief anticipated it, and the M1 data now bears it out. Kyle's call — the documented escalations (position-assigned title pool, same-theme filler generation) remain available, each needing its own addendum |
| D12 | *(Superseded by D16 — its premise was false.)* Paper comparison corrected: the 66.3% Qwen2.5-7B figure is that model's **peak cell** (`absent × synthetic_Y`, Table 2), not our gated cell, and the paper reports no per-cell breakdown for any non-calibration model. The derived claim "paper-contradicting for cheap models" is **withdrawn**. KICKOFF and M0-BRIEF stand as frozen pre-commitments; the correction is recorded, not retrofitted | Superseded (by D16) | 2026-08-03 | Direct re-read of arXiv 2607.09349 §4 / Table 1 / Table 2 / Appendix A on 2026-08-03 — **stopped one appendix short of Appendix C**; `Wiki/Paper-Mapping.md` |
| D13 | *(Superseded by D17.)* **The null is reframed as prior-dependence, not failed replication.** The paper's mechanism is prior-driven ("Stage 1 opens when disease-context overlap activates a parametric attribution prior"); our corpus fabricates both entities *and* all evidence, so no prior about X exists and Stage 1 cannot open. Our design sits **past** `synthetic_Y` on the entity-recognition axis and **off** the paper's grid on the prior axis | Superseded (by D17) | 2026-08-03 | `Wiki/Paper-Mapping.md`; the claim was sized against a 66.3% anchor and does not survive the cell-matched 14% one |
| D14 | *(Superseded by D20 — the recorded rationale was inverted.)* The `synthetic_Y` positive-control arm is **retired** before any build or spend: our corpus already fabricates both entities, which is the manipulation that cell performs | Superseded (by D20) | 2026-08-03 | Kyle's revised call after the paper re-read; recommendation withdrawn by the session that made it |
| D15 | *(Superseded by D19.)* **v1 closes at M1.** M2 (suppress a rate already ~0) and M3 (ablate a phenomenon that occurred twice) are degenerate on the measured data; the cure arm has nothing to cure at DG≈0; the specialization arm has no headroom off a 0–2/20 base and its model is gone from OpenRouter. The prior axis is unreachable judge-free and is stated as a limitation, not attempted. Next: `/research-paper`, then `/seed-hunt` | Superseded (by D19) | 2026-08-03 | Kyle's call, in-session; superseded once D16/D18 showed the premise ("nothing informative remains to run") no longer held |
| D16 | *(Partly superseded by D21 — its "our exact cell" wording.)* **D12's premise was false and is corrected.** The paper's Appendix C (*"Full Cross-Model DG Rate Matrices and Profile Taxonomy"*, Figure 6) publishes complete per-cell DG matrices for **all 13 models**. The **nearest published** cell for our model is **Qwen2.5-7B, `absent × prior_completing` = 14%** (RAG-4 schema) — *not* our cell, see D21; `synthetic_Y` for the same model is 61%. D12's "no cell-matched anchor exists" is withdrawn. D12's *other* correction — that 66.3% is the peak cell, not ours — stands | Superseded in part (by D21) | 2026-08-04 | Caught by adversarial review on PR #10 (F1); verified first-hand by reading `2607.09349v1/figures/fig9_supp_matrices.png` |
| D17 | *(Superseded by D21 — its replacement headline was also wrong.)* **D13's headline is withdrawn.** Against the 14% anchor, both kin-model intervals contain it — stark 0/20 [0.0%, 16.1%], camouflaged 2/20 [2.8%, 30.1%], exact binomial P(X≤2 \| p=0.14) = 0.455. The camouflaged result is **consistent with the paper**, not a refutation, and at N=20/cell the study cannot resolve 14% from 0. The gate's NULL stands as a gate outcome only. Prior-dependence survives as a labelled **Inference** (we sit at `synthetic_Y` on the name axis yet read 0–10% vs its 61%), never as the headline | Superseded (by D21) | 2026-08-04 | PR #10 review F2; intervals recomputed with the repo's own `stats.wilson`; the "consistent with the paper" verb it introduced is itself withdrawn by D21 |
| D18 | **Pre-registration gap, recorded as the project's main finding about itself:** the pre-committed N≥20 was derived from M0's clean-**trial yield** (`m0.py m1_sizing`), never from a power calculation against a target effect size. Against a 14% rate it is underpowered; 0/25 would have been the minimum to exclude it | Approved | 2026-08-04 | PR #10 review F2; `docs/M0-BRIEF.md` Pilot outcome (sizing derivation) vs `Wiki/Paper-Mapping.md` |
| D19 | **RESOLVED — extend.** D15 (close v1) returned to Kyle once D16/D18 changed its premise; his call: run the extension. It was decided on the premise that nothing informative remained to run; D16/D18 change that premise — a pre-registered, power-sized extension on the one model with any published anchor (`qwen-2.5-7b`) would make the **directional** statement decisive. Per D21: 14% is a **reference magnitude for sizing**, never a null hypothesis about our cell; est. ≈$0.06 including the corpus extension (D22). Any such extension is an N-extension after seeing data and is legitimate only if argued in an addendum, fixed in advance, and reported alongside the original N=20 result | Approved | 2026-08-04 | Kyle: "Let's extend once and make it decisive." To be **planned** in a dedicated Fable session before anything is built or spent — the pre-registration is the deliverable, not the wave |
| D20 | **D14's rationale is corrected** (the decision to retire the arm stands; its stated reason did not). `synthetic_Y` has two halves — fabricate Y's *name* **while holding the completing information constant** — and the paper identifies the *information* half as causal (§5.3: *"The model attributes evidence based on information content, not Y's entity-label recognition"*). Our corpus does the name half and cannot do the information half without forfeiting token ownership. Correct rationale: retired because it is **degenerate for us**, not because we "already perform the manipulation". On the recognition axis we are *level with* `synthetic_Y`, never "past" it | Approved | 2026-08-04 | PR #10 review F4 (reopened at round 2); paper §4, §5.3, Appendix A |
| D21 | **D17's replacement headline is also withdrawn — no point comparison to the paper is legitimate.** Three verified reasons: (a) §4/Appendix A *define* `prior_completing` by evidence elicited to match a model's prior for X, which our fabricated tokens cannot do, so the paper has **no cell for our condition**; (b) paper §5.2 body and Appendix C Table 8 state non-L1 completing-Cy absolutes are **lower bounds**, so 14% is a floor (Table 2's *caption* makes the narrower claim about *peak* DG, a different cell); (c) Fig. 6 is RAG-4, Tables 1–2 are 10-tool, and this project ran **neither**. Sensitivity: at p=0.28 the p-value behind "consistent with" falls from 0.455 to 0.053. The relationship is reported as **direction only**, hedged, per the honesty contract's "direction + structure, never point estimates" | Approved | 2026-08-04 | PR #10 review F9/F10 (round 2); all three verified first-hand against the paper |
| D22 | Corrections to superseded rows, recorded rather than retrofitted: D10's "well-powered" wording is withdrawn by D17/D18; D18's "0/25" is **0/24** (Wilson upper 13.80% < 14%); the Wilcoxon statement is paper **§5.3**, not §5.1 R3; `m1_sizing` is a function in `m0.py` called by `cmd_verdict`, not a runnable subcommand; the D19 extension estimate is **≈$0.06**, not ≈$0.02, once the corpus extension and its gen-docs are counted | Approved | 2026-08-04 | PR #10 review F11–F15; costs re-derived from `data/m1b_wave.jsonl` ($0.000121/trial for qwen camouflaged) and `data/gen_log_m1.json` ($0.000133/doc) |
| D23 | **M1C pre-registration committed** (`docs/M1C-BRIEF.md`) — the D19 extension, designed: `qwen-2.5-7b` only, both surfaces, both cells; **combined N=80** per gated cell per surface via a seed-preserving +60-pair corpus extension (p21–p80); primary estimand = Wilson CI on DG-Y at `absent × completing` at combined N, with extension-only (N=60) rows always reported beside the original N=20; one look (`m1c.py verdict` runs once), no further extension regardless of outcome; three pre-committed direction-only report templates carrying the D21 caveats inline; `CAP_M1C_TOTAL = $0.10` on measured spend (est. ≈$0.052). Kyle ruled the three argued points — combined-N primary, full null_control extension, N=80 — each per the drafted recommendation. **Build and spend remain gated on Kyle's explicit go** | Approved | 2026-08-04 | Argued via the drafted brief + a three-question gate in-session; sizing computed with the repo's `stats.wilson` (0/80 upper 4.6%; every reachable k maps to exactly one template); costs re-derived from `data/m1{a,b}_wave.jsonl` and `data/gen_log_m1.json` |
